import styled from "styled-components";

export const Container = styled.section`
  margin: 3rem;

  p {
    font-size: 25px;

    a {
      color: blue;
      text-decoration: underline;
      cursor: pointer;
    }
  }
`;
